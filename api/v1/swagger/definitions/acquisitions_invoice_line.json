{
  "type": "object",
  "required": [
    "id", "order_id", "budget", "item_type", "description", "quantity", "list_price", "discount_rate",
    "discount_amount", "pre_tax_amount", "tax_rate", "tax_amount", "total_price"
  ],
  "properties": {
    "id": {
      "type": ["integer", "null"],
      "description": "Invoice line identifier"
    },
    "order_id": {
      "type": "integer",
      "description": "Order identifier associated with this invoice_line"
    },
    "budget": {
      "type": ["integer", "null"],
      "description": "Budget identifier associated with this invoice_line"
    },
    "item_type": {
      "type": ["integer", "null"],
      "description": "Line item type identifier associated with this invoice_line"
    },
    "description": {
      "type": "string",
      "description": "Description of the charge/discount"
    },
    "quantity": {
      "type": ["integer", "null"],
      "description": "Quantity of 'things' this line covers"
    },
    "list_price": {
      "type": ["number", "null"],
      "description": "Basic line price without discounts or tax applied"
    },
    "discount_rate": {
      "type": ["number", "null"],
      "description": "Percentage used to apply a discount to list_price"
    },
    "discount_amount": {
      "type": ["number", "null"],
      "description": "Amount to apply as discount to list_price"
    },
    "pre_tax_amount": {
      "type": ["number", "null"],
      "description": "Line price after discount has been applied but before tax"
    },
    "tax_rate": {
      "type": ["number", "null"],
      "description": "Percentage used to apply a tax to the pre_tax_amount"
    },
    "tax_amount": {
      "type": ["number", "null"],
      "description": "Amount to apply as tax to list_price"
    },
    "total_price": {
      "type": ["number", "null"],
      "description": "Final price of the line after discounts and tax have been applied"
    }
  }
}
